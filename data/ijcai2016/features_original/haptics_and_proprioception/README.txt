Naming convention:

[behavior]_[modality].txt

The first column corresponds the object ID as integer (1 to 32). The second column denotes the interaction trial (1 to 5). The result of the columns are the sensorimotor features for that particular behavior, modality, object, and trial. 
